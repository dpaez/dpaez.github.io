/* eslint no-unused-vars: ["error", {"args": "none"}] */
const html = require('choo/html');

const spearBottom = (state, prev, send) => {
    return html`
    <div class="spear-bottom">
    </div>`;
};

// spear element
module.exports = spearBottom;
